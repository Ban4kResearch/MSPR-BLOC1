<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de Bord</title>
</head>
<body>

<h1>Tableau de Bord</h1>

<p>Adresse IP locale : {{ local_ip }}</p>
<p>Nom de la machine : {{ hostname }}</p>
<p>Nombre de machines connectées sur le LAN : {{ machines_connectees }}</p>

<h2>Résultats du dernier scan du réseau</h2>
<p>Machines :</p>
<ul>
    {% for host in resultat_scan.hosts %}
        <li>{{ host }}</li>
    {% endfor %}
</ul>

<p>Ports ouverts :</p>
<ul>
    {% for host, port in resultat_scan.open_ports %}
        <li>{{ host }}:{{ port }}</li>
    {% endfor %}
</ul>

<h2>Latence de l'accès WAN</h2>
<p>{{ ping_result }}</p>

<h2>Version de l'application</h2>
<p>{{ version_application }}</p>

</body>
</html>
